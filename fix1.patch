--- src/store.rs
+++ src/store.rs
@@ -191,8 +191,8 @@
         Ok(Self {
             file_path,
             inner: Arc::new(Mutex::new(FileInnerState {
-                tx,
                 next_global_seq,
+                tx,
                 id_index,
             })),
         })
