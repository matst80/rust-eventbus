apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app
  labels:
    app: todo-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      containers:
      - name: todo-app
        image: matst80/rust-eventbus-todo:latest # Replace with your actual image
        ports:
        - name: api
          containerPort: 3000
        - name: mesh
          containerPort: 3001
        env:
        - name: PORT
          value: "3000"
        - name: HOST
          value: "0.0.0.0"
        - name: MESH_PORT
          value: "3001"
        - name: DATA_DIR
          value: "/app/data"
        - name: RUST_LOG
          value: "rust_eventbus=info,todo_app=info"
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: DNS_QUERY
          value: "todo-app-headless.$(POD_NAMESPACE).svc.cluster.local"
        - name: NODE_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.uid
        readinessProbe:
          tcpSocket:
            port: 3001
          initialDelaySeconds: 3
          periodSeconds: 5
          timeoutSeconds: 2
        livenessProbe:
          tcpSocket:
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 2
        startupProbe:
          tcpSocket:
            port: 3001
          failureThreshold: 30
          periodSeconds: 2
          timeoutSeconds: 2
        volumeMounts:
        - name: data-storage
          mountPath: /app/data
      terminationGracePeriodSeconds: 30
      nodeSelector:
        kubernetes.io/arch: arm64
      volumes:
      - name: data-storage
        persistentVolumeClaim:
          claimName: todo-app-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-app-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: todo-app-headless
spec:
  clusterIP: None # Headless Service for DNS discovery
  selector:
    app: todo-app
  ports:
    - name: mesh
      protocol: TCP
      port: 3001
      targetPort: 3001
---
apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
spec:
  selector:
    app: todo-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-app-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - todo.k6n.net
    secretName: todo-app-tls
  rules:
  - host: todo.k6n.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: todo-app-service
            port:
              number: 80
